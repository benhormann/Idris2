case $(uname) in
    *BSD | DragonFly) MAKE='gmake' ;;
    *) MAKE='make' ;;
esac

${MAKE} all >/dev/null
$1 --no-color --console-width 0 --no-banner CB.idr <input

rm -rf build
${MAKE} clean >/dev/null
